# .dotfiles

My configuration files.
(Un)installation scripts require `stow`.

## Install

```shell
./install

# dry run (see commands)
./install dry
```

## Uninstall

```shell
./uninstall

# dry run (see commands)
./uninstall dry
```


